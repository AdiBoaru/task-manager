export type TProjectsData = {
    creationDate: string,
    description: string,
    dueDate: Date,
    employees: string[],
    id: number,
    tasks: string[],
    teamSize: number,
    title: string,
}