package java.com.togbo.taskmanager.services;

import com.togbo.taskmanager.model.Team;
import com.togbo.taskmanager.repository.TeamRepository;
import com.togbo.taskmanager.services.EmployeeService;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;
import org.mockito.Mock;
import org.mockito.MockitoAnnotations;

import static org.mockito.Mockito.mock;
import static org.mockito.Mockito.when;

public class TeamServiceTest {
/*
    @Mock
    private final TeamRepository teamRepository;

    @Mock
    private final EmployeeService employeeService;
    AutoCloseable autoCloseable;
    @Mock
    Team team;


    @BeforeEach
    void setUp(){
        autoCloseable = MockitoAnnotations.openMocks(this);
    }

    @Test
    void createTeam(){
        mock(Team.class);
        mock(TeamRepository.class);

        Team teamPresent = teamRepository.findByName(team.getName());

        when(teamRepository.save(team));
    }


 */
}
