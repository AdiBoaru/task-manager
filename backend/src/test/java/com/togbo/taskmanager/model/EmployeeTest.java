package com.togbo.taskmanager.model;

import com.togbo.taskmanager.enums.Role;
import org.junit.jupiter.api.Assertions;
import org.junit.jupiter.api.Test;

import java.time.LocalDate;

import static org.junit.jupiter.api.Assertions.*;

class EmployeeTest {
  /*  @Test
    public void modelEmployeeTest(){
        Account account = new Account("dragos@t.com", "123", LocalDate.now());
        Employee employee = new Employee("martin", "firav",
                LocalDate.of(1995, 10, 10), Role.TESTER, account);

        Assertions.assertTrue(employee.getFirstName().equals("martin")
                && employee.getLastName().equals("firav")
                && employee.getBirthDate().equals(LocalDate.of(1995, 10, 10))
                && employee.getRole().equals(Role.TESTER)
                && employee.getAccount().equals(account));
    }

   */
}